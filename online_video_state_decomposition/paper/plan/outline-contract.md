# Outline Contract

The manuscript remains hypothesis-safe until the planned probes and system experiments are complete. Total target is 35-50 verified citations and three primary figures; no numerical result enters the abstract without a backing CSV or JSON artifact.

1. **Introduction**
   - Establish causality, accumulation, rare-event retention, and tail latency as the online-video problem.
   - State the exact-recent versus consolidated-memory allocation question and its falsifiable alternatives.
   - Separate state decomposition from attention-matrix fitting.
   - Citation quota: 5-7. Figure quota: one method overview.

2. **Evidence and Problem Reframing**
   - Summarize verified negative BCCB replacement evidence, conditional Wan geometry, dynamic route transfer failure, and SCTM route causality.
   - Explain why DRE-BCM weight residuals are not temporal-memory evidence.
   - Citation quota: 4-6. Table quota: one verified-evidence boundary table.

3. **Related Work and Novelty Boundary**
   - Structured attention: Circulant Attention, MonarchAttention, VMonarch, MonarchRT, RoPeSLR.
   - Streaming bounded memory: Flash-VStream, VideoChat-Online, LiveVLM, StreamingTOM, StateKV, CausalMem.
   - Event memory: Event-VStream and EventMemAgent.
   - State/decomposition antecedents: VideoMamba, online robust PCA, dynamic mode decomposition.
   - Citation quota: 14-20. No figure required.

4. **Two-Timescale Fixed-Budget Memory**
   - Define the exact instant cache, causal low-dimensional memory, readout, and byte allocation.
   - State computational and memory bounds.
   - Distinguish memory-only, instant-only, and hybrid updates.
   - Citation quota: 3-5. Figure quota: one detailed state-update diagram.

5. **Probe Methodology**
   - Temporal effective-rank probe.
   - Sparse-innovation concentration and event-alignment probe.
   - Structured transport fit and activation intervention probe.
   - Matched-byte memory-retention and instant-window allocation probe.
   - Pre-registered go, pivot, and kill criteria, including the failed event and BCCB paths.
   - Citation quota: 3-5. Figure quota: one three-panel probe figure.

6. **End-to-End System and Implementation**
   - Frozen-base MVP, instant-cache read, subspace update/readout, and task interface.
   - Batched memory operations and complete cache accounting.
   - Timing protocol and SLO metrics.
   - Citation quota: 3-5. Table quota: one complexity/accounting table.

7. **Evaluation**
   - Current-scene and delayed-query accuracy under matched state bytes and compute.
   - Instant-frame versus subspace-rank allocation, memory update ablations, and cross-encoder transfer.
   - P50/P95/P99 latency, peak memory, state bytes per hour, and update/read overhead.
   - Citation quota: 2-4. Figure quota: Pareto and tail-latency plots.

8. **Limitations and Conclusion**
   - Report failed probes, weak regimes, archive growth risks, and generation-transfer limits.
   - Conclude only with verified measurements.
   - Citation quota: 1-2.

## Required Main-Paper Visuals

- Fig. 1: causal two-timescale memory update under a fixed byte budget.
- Fig. 2: temporal-rank, failed innovation/transport probes, and memory-retention results.
- Fig. 3: task quality versus state bytes and measured P95 latency.
- Table 1: closest prior methods versus transport, bounded state, event memory, and tail-latency evaluation.
- Table 2: matched-budget memory comparison and instant/subspace allocation ablations.
