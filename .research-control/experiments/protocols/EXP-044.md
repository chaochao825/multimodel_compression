# EXP-044: Balanced finite-jump trainability decomposition

- Status: complete
- Claim: C-023
- Candidate: L-023
- Gate: G-023
- Kind: post-outcome development diagnostic

## Data boundary

- Reuse immutable EXP-043 payloads for `dev00_cloth` and `dev01_turntable`.
- Both identities are outcome-exposed development data.
- Calibration, validation, test, and any new prompt or seed are prohibited.
- Verify payload and manifest hashes before training.

## Frozen training

- FP32 adapter and state tensors on one exclusive H200.
- 8,000 final-checkpoint-only steps per run, AdamW, learning rate 1e-3, no
  weight decay, seed 2026081344.
- Deterministic stratified interval schedule: exactly 2,000 updates per interval.
- Four frames and one 16x16 crop per step.
- Loss is crop MSE divided by the full interval target mean-square energy.

## Frozen model families

- Shared structured lifting: 28,160 parameters.
- Interval-FiLM structured lifting: 28,416 parameters.
- Four stage-specific structured liftings: 112,640 parameters.
- Four stage-specific width-128 liftings: 450,560 parameters.

## Frozen runs

- `shared_observable_transfer`
- `shared_clean_transfer`
- `film_observable_transfer`
- `stage_observable_transfer`
- `stage_target_transfer`
- `stage_observable_transductive`
- `stage_target_transductive`
- `wide_stage_target_transductive`

Transfer trains `dev00_cloth` and evaluates both development identities.
Transductive trains and evaluates `dev01_turntable`; it is a capacity-only
diagnostic and cannot support generalization.

## Metrics and outcome

- Per-interval curvature relative L2.
- Displacement-weighted curvature relative L2.
- Frozen-field endpoint and prefix relative L2.
- Parameter count, fixed-step training time, and standalone adapter latency.
- Outcome mapping is frozen in PLAN-052.

## Stop rule

Stop after one valid eight-run result, one permitted pre-metric implementation
repair, hash/integrity failure, foreign-process overlap, or resource failure.
No additional width, rank, loss, optimizer, identity, or rollout is allowed.

## Closure

- Outcome: `local-function-null`.
- Result: `.research-control/results/RESULT-EXP-044.md`.
- All eight frozen runs completed with exact balanced interval counts and valid
  artifact hashes. No post-outcome arm was added.
