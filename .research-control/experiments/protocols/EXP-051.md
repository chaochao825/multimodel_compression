# EXP-051: True joint support-state capacity

- Status: completed
- Claim: C-029
- Candidate: L-029
- Gate: G-030
- Lane: explore side probe

## Decision question

When the mergeable width-32 state is trained under the exact support that its
own residual induces, does the final joint model achieve a capacity tier that
cannot be explained by support selection or state adaptation alone?

## Frozen scope

- Frozen LLaVA-OneVision Qwen2-7B reader and repaired exact-page capture.
- Calibration positions 1--72; exposed development positions 73--96.
- Positions 97--120, official selection, and formal roles remain unread.
- Layers 0/13/27; eight frames; 1,568 visual tokens.
- Four-token regular pages; 25% exact tokens; feature width 32.
- Same whole-state checkpoint initialization for both trainable arms.
- AdamW, 1,000 steps per arm per layer, batch size 2, learning rate `3e-4`,
  seed 20260901, identical precomputed calibration batch schedule.

## Methods

Primary four-arm comparison:

1. exact-only mass support;
2. frozen state-only;
3. mass-support-trained state with mass correction;
4. residual-support-trained state with residual correction.

Factorial diagnostics:

5. mass-support-trained state with residual correction;
6. residual-support-trained state with mass correction.

All corrections use the same control-variate numerator/denominator algebra.
Development residual support reads the target and is a function-class ceiling,
not a router.

## G-030

The joint arm passes only if all hold:

- 72 complete development cells;
- visual mean/P95/worst relative L2 at most 0.5%/1%/2%;
- paired risk improvement over independent mass correction has bootstrap lower
  95% bound at least 25%;
- paired improvement over independent-state residual support has lower bound
  above zero;
- paired improvement over joint-state mass support has lower bound above zero;
- analytic active-state ratio at least 3x;
- exact page counts, finite gradients, state replay, and all-page dense recovery
  pass.

## Stop rule

Use one A800 and at most two implementation repairs. Stop on one valid G-030
outcome, leakage, identity drift, nonfinite training, two failed repairs,
resource exhaustion, or user interruption. Do not open task intervention,
router training, confirmation, formal, or H200 timing after a miss.
