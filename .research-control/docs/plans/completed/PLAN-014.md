# PLAN-014: Test shared centered-latent amortization

- Status: completed
- Owner: researcher and Agent
- Gate: G-014
- Claim: C-014
- Candidate: L-014
- Lane: explore
- Resource cap: two existing captures, at most two H200-hours, one valid
  execution, and one pre-outcome engineering repair

## Decision to unlock

Determine whether output-channel subspaces can be shared across query tiles and
generated by a centered K/V latent family cheaply enough to create real margin
below the EXP-013 61.33% cost boundary.

## Milestones

1. Freeze nested tile selection, support, ranks, centered family, optimizer,
   quality/recovery gates, and amortized cost model.
2. Implement and test centered-anchor algebra, shared versus independent
   projection, deterministic tile nesting, cost accounting, and frozen
   validation-to-test `(M, rank)` selection.
3. Run Stage A and Stage B once on the existing H200 captures while monitoring
   GPU/process progress and preserving all raw artifacts.
4. Visualize rank growth, quality-cost frontiers, adaptive recovery, and the
   frozen test decision; close G-014 before any predictor or kernel work.

## Stop rules

- Do not add a basis family, rank, tile count, restart, or optimizer setting
  after outcomes are visible.
- Dense-defect latent queries and coefficients remain oracle-only.
- Stop after one valid terminal outcome or the one allowed pre-outcome repair.
- Do not train a deployable router/basis predictor or run rollout in this Gate.
