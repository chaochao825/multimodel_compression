# PLAN-002: Decide whether structure can certify cheap attention actions

- Status: completed; G-004 closed as null
- Owner: researcher and Agent
- Gate: G-004
- Claims: C-004
- Candidate line: L-004
- Lane: explore
- Resource cap: one pass over 36 existing QKV captures, one frozen analysis,
  and one pre-outcome engineering repair

## Decision to unlock

Decide whether the original local/frequency structure intuition is useful as a
low-dimensional risk sensor after failing as a high-dimensional output basis.

## Context and source authority

- Project authority: `.research-control/PROJECT.md` and `STATUS.md`.
- Accepted decision: `RDR-0004`.
- Frozen protocol: `EXP-004`.
- Existing QKV capture and centroid/moment worker artifacts on server 236.
- Configuration identity is recorded before scientific execution.

## Non-goals

- No new BCM, Butterfly, low-rank tail, sparse worker, model capture, training,
  kernel, rollout, or H200 speed claim.
- No test-dependent threshold, feature, action, or regularization selection.
- No claim that local AV error proves final-video fidelity.

## Milestones

1. Validate source coverage, split disjointness, and the target-leaking action
   envelope before fitting a scout.
2. Extract the frozen linear-cost Q/K/V features once and record provenance.
3. Fit calibration-only scores, freeze thresholds on validation, and evaluate
   the two test identities once.
4. Produce a decision report and close `G-004` under the protocol mapping.

## Validation

Unit-test SICS, finite-difference, ridge, selection, and split guards. Require
deterministic output, finite values, exact action-grid coverage, immutable test
membership, and strict Research Control Plane validation.

## Stop and escalation rules

Stop on the first terminal EXP-004 outcome or resource cap. Any learned router,
new approximation operator, kernel, rollout, or project-mainline change needs a
new researcher decision.

## Closure

Update the result record, registries, `STATUS.md`, and this plan. Preserve null,
adverse, boundary, and invalid artifacts.

## Decision outcome

The target-leaking multi-action envelope reached 1.554x arithmetic attention
speed, but the frozen full scout reached only 1.043x and did not improve the
1.324x static baseline. G-004 closed as null without rollout or kernel work.
